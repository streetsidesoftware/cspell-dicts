# Add additional suggestions here.
# the form is:
# misspelling->suggestion
#
# Items prefixed with `!` will be placed in `flagWords` section of the dictionary.
#
# cspell:locale en
#
# Tell cspell to not spell check the misspelled words:
# cspell:ignoreRegExp /\b[\p{L}'-]+->/gmu
!curch->church
!programing->programming
!therefor->therefore # `therefor` is archaic and a common misspelling of therefore. // cspell:ignore therefor
acitve->active
colum->column
doens't->doesn't
enterprize->enterprise
enterprizes->enterprises
idealogue->ideologue
peventing->preventing
pocessing->processing
programatic->programmatic
remnat->remnant
remnats->remnants
shis->this
signiory->seigniory
signory->seigniory
typw->type
witten->written
